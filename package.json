{
  "name": "healthlink-clinic",
  "private": true,
  "description": "Racine du monorepo : frontend/ (React) et backend/ (API Express).",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "install:all": "npm install",
    "dev": "npm run dev -w frontend",
    "dev:backend": "npm run dev -w backend",
    "dev:all": "concurrently \"npm run dev -w frontend\" \"npm run dev -w backend\" --names \"frontend,backend\" --prefix \"[{name}]\" --colors",
    "build": "node node_modules/vite/bin/vite.js build frontend",
    "start:backend": "npm run start -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "vite": "^7.3.1",
    "typescript": "^5.8.3"
  }
}
